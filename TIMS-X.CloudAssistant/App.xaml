<Application x:Class="TIMS_X.CloudAssistant.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TIMS_X.CloudAssistant"
             xmlns:converters="clr-namespace:TIMS_X.CloudAssistant.Converters"
             Startup="Application_Startup">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- MahApps.Metro resource dictionaries. Make sure that all file names are Case Sensitive! -->
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Colors.xaml" />
                <!-- Accent and AppTheme setting -->
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/Steel.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/BaseLight.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <converters:BoolColorConverter x:Key="BoolColorConverter" />

            <SolidColorBrush x:Key="TIMSBlue">#003768</SolidColorBrush>

            <Style x:Key="LargeLabelStyle" TargetType="{x:Type TextBlock}">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="#FFEEEEEE"/>
            </Style>

            <Style x:Key="LabelStyle" TargetType="{x:Type TextBlock}">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="Foreground" Value="#FFEEEEEE"/>
            </Style>

            <Style x:Key="SmallLabelStyle" TargetType="{x:Type TextBlock}">
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="Foreground" Value="#FFEEEEEE"/>
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>

            <Style x:Key="CloseButtonStyle" TargetType="{x:Type Button}">
                <Style.Resources>
                    <SolidColorBrush x:Key="NormalBackgroundBrush" Color="#00000000" />
                    <SolidColorBrush x:Key="NormalBorderBrush" Color="#FFFFFFFF" />
                    <SolidColorBrush x:Key="NormalForegroundBrush" Color="#FF8f949b" />

                    <SolidColorBrush x:Key="HoverBackgroundBrush" Color="#FFc13535" />
                    <SolidColorBrush x:Key="HoverForegroundBrush" Color="#FFf9ebeb" />

                    <SolidColorBrush x:Key="PressedBackgroundBrush" Color="#FF431e20" />
                    <SolidColorBrush x:Key="PressedBorderBrush" Color="#FF110033" />
                    <SolidColorBrush x:Key="PressedForegroundBrush" Color="#FFf9ebeb" />
                </Style.Resources>
                <!-- Simple properties that we set. -->
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Focusable" Value="False" />
                <Setter Property="Width" Value="15" />
                <Setter Property="Height" Value="15" />
                
                <!-- The control template. -->
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Grid Background="Transparent">

                                <!-- The background of the button, as an ellipse. -->
                                <Ellipse x:Name="backgroundEllipse" />

                                <!-- A path that renders a cross. -->
                                <Path x:Name="ButtonPath"

                                      Margin="3"

                                      Stroke="{Binding Path=IsSelected, 
                                                       RelativeSource={RelativeSource AncestorType={x:Type TabItem}}, 
                                                       Converter={StaticResource BoolColorConverter}, 
                                                       UpdateSourceTrigger=PropertyChanged}"

                                      StrokeThickness="1.5"

                                      StrokeStartLineCap="Square"

                                      StrokeEndLineCap="Square"

                                      Stretch="Uniform"

                                      VerticalAlignment="Center"

                                      HorizontalAlignment="Center">
                                    <Path.Data>
                                        <PathGeometry>
                                            <PathGeometry.Figures>
                                                <PathFigure StartPoint="0,0">
                                                    <LineSegment Point="25,25"/>
                                                </PathFigure>
                                                <PathFigure StartPoint="0,25">
                                                    <LineSegment Point="25,0"/>
                                                </PathFigure>
                                            </PathGeometry.Figures>
                                        </PathGeometry>
                                    </Path.Data>
                                    
                                </Path>
                            </Grid>
                            <!-- The triggers. -->
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="backgroundEllipse" Property="Fill"

                                            Value="{StaticResource HoverBackgroundBrush}" />
                                    <Setter TargetName="ButtonPath" Property="Stroke" 

                                            Value="{StaticResource HoverForegroundBrush}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="false">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="true">
                                    <Setter TargetName="backgroundEllipse" Property="Fill"

                                            Value="{StaticResource PressedBackgroundBrush}" />
                                    <Setter TargetName="backgroundEllipse" Property="Stroke"

                                            Value="{StaticResource PressedBorderBrush}" />
                                    <Setter TargetName="ButtonPath" Property="Stroke" 

                                            Value="{StaticResource PressedForegroundBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="CloseButtonStyle2" TargetType="{x:Type Button}">
                <Style.Resources>
                    <SolidColorBrush x:Key="NormalBackgroundBrush" Color="Silver" />
                    <SolidColorBrush x:Key="NormalBorderBrush" Color="#666666" />
                    <SolidColorBrush x:Key="NormalForegroundBrush" Color="#222222" />

                    <SolidColorBrush x:Key="HoverBackgroundBrush" Color="#FFc13535" />
                    <SolidColorBrush x:Key="HoverForegroundBrush" Color="#FFf9ebeb" />

                    <SolidColorBrush x:Key="PressedBackgroundBrush" Color="#FF431e20" />
                    <SolidColorBrush x:Key="PressedBorderBrush" Color="#FF110033" />
                    <SolidColorBrush x:Key="PressedForegroundBrush" Color="#FFf9ebeb" />
                </Style.Resources>
                <!-- Simple properties that we set. -->
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Focusable" Value="False" />
                <Setter Property="Height" Value="25" />

                <!-- The control template. -->
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <StackPanel Orientation="Horizontal">
                                <Grid Background="Transparent" x:Name="BackgroundGrid">

                                    <!-- A path that renders a cross. -->
                                    <Path x:Name="ButtonPath"

                                      Margin="3"
                                      Width="15"
                                      Height="15"
                                      Stroke="{Binding Path=IsSelected, 
                                                       RelativeSource={RelativeSource AncestorType={x:Type TabItem}}, 
                                                       Converter={StaticResource BoolColorConverter}, 
                                                       UpdateSourceTrigger=PropertyChanged}"

                                      StrokeThickness="1.5"

                                      StrokeStartLineCap="Square"

                                      StrokeEndLineCap="Square"

                                      Stretch="Uniform"

                                      VerticalAlignment="Center"

                                      HorizontalAlignment="Center">
                                        <Path.Data>
                                            <PathGeometry>
                                                <PathGeometry.Figures>
                                                    <PathFigure StartPoint="0,0">
                                                        <LineSegment Point="25,25"/>
                                                    </PathFigure>
                                                    <PathFigure StartPoint="0,25">
                                                        <LineSegment Point="25,0"/>
                                                    </PathFigure>
                                                </PathGeometry.Figures>
                                            </PathGeometry>
                                        </Path.Data>

                                    </Path>
                                </Grid>
                                <!--<TextBlock Text="Close Patient" Margin="5,2"></TextBlock>-->
                                <ContentPresenter ContentSource="Content" Margin="5,2"></ContentPresenter>
                            </StackPanel>
                            
                            <!-- The triggers. -->
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="BackgroundGrid" Property="Background"

                                            Value="{StaticResource HoverBackgroundBrush}" />
                                    <Setter TargetName="ButtonPath" Property="Stroke" 

                                            Value="{StaticResource HoverForegroundBrush}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="false">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="true">
                                    <Setter TargetName="BackgroundGrid" Property="Background"

                                            Value="{StaticResource PressedBackgroundBrush}" />
                                    <Setter TargetName="BackgroundGrid" Property="Background"

                                            Value="{StaticResource PressedBorderBrush}" />
                                    <Setter TargetName="ButtonPath" Property="Stroke" 

                                            Value="{StaticResource PressedForegroundBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
