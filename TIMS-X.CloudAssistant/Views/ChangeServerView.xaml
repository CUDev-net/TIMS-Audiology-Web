<Window x:Class="TIMS_X.CloudAssistant.Views.ChangeServerView"
        xmlns:md="https://github.com/fantasticfiasco/mvvm-dialogs"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TIMS_X.CloudAssistant.Views"
        xmlns:converters="clr-namespace:TIMS_X.CloudAssistant.Converters"
        mc:Ignorable="d"
        md:DialogServiceViews.IsRegistered="True"
        Closing="Window_Closing"
        Title="Configure Tims Server Connection" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style x:Key="PromptTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="HeaderTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="ErrorTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="Red" />
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,0,5" />
        </Style>
        <Style x:Key="ValueTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="7,2"></Setter>
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Padding" Value="5,2"></Setter>
        </Style>
        <Style x:Key="ValueTextStyle2" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="#3D3D3D" />
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="7,2"></Setter>
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Padding" Value="5,2"></Setter>
        </Style>
        <Style x:Key="ContactTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <converters:NoahButtonTextConverter x:Key="NoahButtonTextConverter" />
        <converters:StatusColorConverter x:Key="StatusColorConverter" />
        <converters:StatusTextConverter x:Key="StatusTextConverter" />
    </Window.Resources>

    <StackPanel Orientation="Vertical" Margin="25">
        <TextBlock x:Name="ErrorMessage" Visibility="Collapsed" Style="{StaticResource ErrorTextStyle}"/>
        <TextBlock Text="Server Url:" Style="{StaticResource HeaderTextStyle}" HorizontalAlignment="Left"/>
        <TextBox Width="320" Text="{Binding ServerUrl}" HorizontalAlignment="Left" />
        <TextBlock Text="Port:" Style="{StaticResource HeaderTextStyle}" HorizontalAlignment="Left" Margin="0,5,0,0"/>
        <TextBox Width="120" Text="{Binding Port}" HorizontalAlignment="Left" />
        <TextBlock Text="Office Code:" Style="{StaticResource HeaderTextStyle}" HorizontalAlignment="Left" Margin="0,5,0,0"/>
        <TextBox Width="120" Text="{Binding OfficeCode}" HorizontalAlignment="Left" />
        <CheckBox Content="Use AD Authentication" IsChecked="{Binding UseADAuthentication}" Margin="0,10,0,0" FontWeight="SemiBold"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Cancel" Command="{Binding CancelCommand}" HorizontalAlignment="Right" Margin="5" Width="70"/>
            <Button Content="Restore Defaults" Command="{Binding RestoreDefaultsCommand}" HorizontalAlignment="Right" Margin="5" Width="110"/>
            <Button Content="Save" Command="{Binding SaveCommand}" Background="DodgerBlue" Foreground="WhiteSmoke" HorizontalAlignment="Right" Margin="5" Width="70"/>
        </StackPanel>
        
    </StackPanel>
</Window>
