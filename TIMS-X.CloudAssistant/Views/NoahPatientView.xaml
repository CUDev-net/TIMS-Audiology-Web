<UserControl x:Class="TIMS_X.CloudAssistant.Views.NoahPatientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TIMS_X.CloudAssistant.Views"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:converters="clr-namespace:TIMS_X.CloudAssistant.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <Style x:Key="TextBlockStyle" TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="#333333" />
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="VerticalAlignment" Value="Center"></Setter>
            </Style>
            <Style x:Key="CloseButtonTextStyle" TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="#333333" />
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="VerticalAlignment" Value="Center"></Setter>
            </Style>
            <Style x:Key="DateStyle" TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="#333333" />
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </Style>
            <converters:BoolVisibilityConverter x:Key="BoolVisibilityConverter" />
            <converters:LoadTimeColorConverter x:Key="LoadTimeColorConverter" />
            <converters:BoolVisibilityInverter x:Key="BoolVisibilityInverter" />
        </ResourceDictionary>

    </UserControl.Resources>

        <Grid Background="Transparent" Visibility="{Binding NoahTestingVisibility, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Border BorderThickness="1" BorderBrush="Silver" Background="WhiteSmoke" Padding="15,5">
                <DockPanel>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Image Source="{Binding PatientAvatarPath, UpdateSourceTrigger=PropertyChanged}" Width="35" Height="35" Grid.Row="0" Grid.RowSpan="2"></Image>
                        <TextBlock Text="{Binding PatientName}" Style="{StaticResource TextBlockStyle}" Margin="5" Grid.Row="0" Grid.Column="1"></TextBlock>
                        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1">
                            <TextBlock Text="DOB:" Style="{StaticResource DateStyle}" Margin="5,0"></TextBlock>
                        <TextBlock Text="{Binding Model.BirthDate, StringFormat=d}" Style="{StaticResource DateStyle}" Margin="0,0,5,5"></TextBlock>

                    </StackPanel>
                    </Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right" >
                    <Button x:Name="CloseButton" ToolTip="Close Patient Record" Foreground="#FF8f949b" Margin="4" HorizontalAlignment="Right" Height="30" Command="{Binding CloseCommand}" DockPanel.Dock="Right">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <Path
                                Width="12"
                                Height="12"
                                Stroke="Black"
                                StrokeThickness="1.5"
                                StrokeStartLineCap="Square"
                                StrokeEndLineCap="Square"
                                Stretch="Uniform"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center">
                                    <Path.Data>
                                        <PathGeometry>
                                            <PathGeometry.Figures>
                                                <PathFigure StartPoint="0,0">
                                                    <LineSegment Point="25,25"/>
                                                </PathFigure>
                                                <PathFigure StartPoint="0,25">
                                                    <LineSegment Point="25,0"/>
                                                </PathFigure>
                                            </PathGeometry.Figures>
                                        </PathGeometry>
                                    </Path.Data>
                                </Path>
                                <TextBlock Style="{StaticResource CloseButtonTextStyle}" Text="Close" Margin="4,0"></TextBlock>
                            </StackPanel>
                        </Button.Content>
                    </Button>
                </StackPanel>
                

                </DockPanel>
            </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Grid.Row="1">
            <Grid >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Row="0" x:Name="ExportButton" Width="140" HorizontalAlignment="Left" ToolTip="Export patient's noah data to nhax file" Margin="34,8,0,0" Height="30" Command="{Binding ExportCommand}"
                            Content="Export Noah Data">

                </Button>
                <Grid x:Name="PART_ModuleContainer" Grid.Row="1"></Grid>

                <Grid Background="Transparent" Grid.Row="2">
                    <StackPanel Orientation="Horizontal" Margin="15" HorizontalAlignment="Center" Visibility="{Binding IsBusy, Converter={StaticResource BoolVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}" >
                        <mah:ProgressRing 
                                IsActive="{Binding IsBusy, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                Width="30" Height="30" />
                        <TextBlock Margin="15" Style="{StaticResource DateStyle}" Text="Loading, Please Wait..." VerticalAlignment="Center" />

                    </StackPanel>
                    <Grid Margin="25,10" x:Name="PART_SessionContainer" Visibility="{Binding IsBusy, Converter={StaticResource BoolVisibilityInverter}, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Stretch"></Grid>
                </Grid>

            </Grid>
        </ScrollViewer>

    </Grid>



    
</UserControl>
